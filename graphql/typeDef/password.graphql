input ChangePassword {
    email: String
    oldPassword: String
    password: String
}

input Email {
    email: String
}

type Mutation {
    change_password(input: ChangePassword): response
    reset_password(input: Email): response
}